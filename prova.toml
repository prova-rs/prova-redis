# Self-test manifest: resolve this plugin from its own directory (via prova-plugin.toml) and run
# tests/. A consumer would instead declare `redis = "prova-rs/prova-redis@v1"`.
[run]
paths = ["tests"]

[plugins]
redis = { path = "." }
